# Скопируйте в .env (не коммитится). Без .env проект работает на SQLite с DEBUG=1.
DEBUG=1
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=127.0.0.1,localhost
# Postgres (необязательно локально): postgres://user:pass@localhost:5432/django2026
DATABASE_URL=
